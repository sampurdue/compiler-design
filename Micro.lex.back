DIGIT [0-9]
ID [a-zA-Z][a-zA-Z0-9]*


float {DIGIT}+"."{DIGIT}+
INTLITERAL {DIGIT}+




%%
{INTLITERAL} 	printf("Found intliteral %s\n", yytext);
		printf(" what the hell");



{float}
		printf("flot found : %s",yytext);
%%


int main(int argc, char *argv[])
{
	if(argc < 2)
	printf("This needs a file");
	else
	{
		FILE *sourceFile = fopen(argv[1], "r");
		if(sourceFile == NULL)
			printf("File not found");
		else
			yyin = sourceFile;
	}
	yylex();
}
